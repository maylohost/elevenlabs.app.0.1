# Dipendenze per Voice Converter ElevenLabs
# Installa con: pip install -r requirements.txt

# Audio recording and playback
sounddevice>=0.5.0
soundfile>=0.12.0

# HTTP requests per API ElevenLabs
requests>=2.32.0

# Processing audio data
numpy>=2.0.0

# tkinter è incluso in Python standard ma su Linux potrebbe richiedere:
# sudo apt install python3-tk

